const should = require('should')
const mongoose = require("mongoose");
const nconf = require("nconf")

module.exports = function(server) {
  describe("Testing Two Factor Scenario : ", function() {

    it("Tesing 2FA By Passing :twoFactorToken In The URL Header, It Should Not Show Error");

  });
}
