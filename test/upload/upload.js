const should = require('should')
const mongoose = require("mongoose");
const nconf = require("nconf")

module.exports = function(server) {
  describe("Upload Documents Scenario : ", function() {

    it("Missing Data, It Should Return Error");

    it("Missing Nothing, It Should Not Return Any Error");


  });
}
